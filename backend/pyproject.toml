[project]
name = "foodlogr"
version = "0.1.0"
description = "Persistent food logging MCP server for Claude"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"
